.model small
.stack 100h
.data
    num1 db 25h
    num2 db 40h
.code
main:
    mov ax, @data
    mov ds, ax

    mov al, num1
    mov bl, num2
    call SWAP

    mov num1, al
    mov num2, bl

    mov ah, 4ch
    int 21h

SWAP proc
    xchg al, bl
    ret
SWAP endp
end main