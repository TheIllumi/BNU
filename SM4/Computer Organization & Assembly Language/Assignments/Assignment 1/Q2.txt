.model small
.stack 100
.data

.code
main proc
    mov ax, 7E7Eh
                               
    mov cx, 16                 
    mov dx, 1                  
    mov si, ax                
    mov di, ax                

    xor bx, bx                
    mov cx, 16
reverse_loop:
    shl di, 1                 
    rcr bx, 1                 
    loop reverse_loop

    cmp si, bx
    jne not_palindrome

    mov dx, 1
    jmp done

not_palindrome:
    mov dx, 0

done:
    mov ah, 4ch
    int 21h


main endp
end main